import 'package:flutter/material.dart';

class LoudingIndicatorWidget extends StatelessWidget {
  const LoudingIndicatorWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
